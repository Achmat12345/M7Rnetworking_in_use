# Netlify redirects file for React Router SPA
# This ensures all routes are handled by the client-side router
# instead of returning 404 errors

# Redirect all requests to index.html with 200 status (SPA fallback)
/*    /index.html   200

# Optional: Redirect API calls to your backend (if needed)
# /api/*  https://your-backend-url.com/api/:splat  200

# Optional: Force HTTPS redirect
# http://m7rnetworking.netlify.app/*  https://m7rnetworking.netlify.app/:splat  301!

# Optional: Redirect old URLs or specific routes
# /old-route  /new-route  301
# /legacy/*   /new-path/:splat  301
